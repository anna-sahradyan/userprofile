import React from 'react';
import * as Loader from "react-loader-spinner";

const Loading = () => {
    return (
        <>
            <div>
                <Loader.Audio
                />
            </div>
        </>
    );
};

export default Loading;